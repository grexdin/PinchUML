1. Run `npm run lint` or `npm run typecheck` to ensure the codebase remains healthy.
2. Check `git status` for uncommitted changes that might need a descriptive commit message.
3. Report any regressions or build warnings silently introduced during the session.
